AnyEvent-HTTP-Server

This branch is with legacy version, that was firstly introduced.
I know, that some people using it.
But since it is rather slow in comparison to new version,
and since it was not released yet to CPAN, I moved it to a branch and rewrite master with new version